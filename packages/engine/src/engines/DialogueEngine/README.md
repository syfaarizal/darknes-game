# engine/engines/DialogueEngine

Typewriter reveal timing and history-entry construction. `runTypewriter` is cancellable so a click can jump straight to full text; speed is read from `settingsStore.textSpeedPreset`.
