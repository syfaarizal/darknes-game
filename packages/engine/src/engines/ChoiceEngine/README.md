# engine/engines/ChoiceEngine

Filters a choice node's options by their conditions (a locked-out option simply doesn't render) and applies the chosen option's flag/variable mutations before handing back the next node id.
