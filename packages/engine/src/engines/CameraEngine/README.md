# engine/engines/CameraEngine

Converts a scene node's `CameraInstruction` (zoom, pan, shake, punch) into Framer-Motion-shaped `animate`/`transition` props, so this package stays free of a `motion` dependency.
