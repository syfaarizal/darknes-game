# engine/engines/AnimationEngine

Shared transition timing/easing tokens (fade, cross-fade, slide, flash, cut) so scene transitions look identical whether triggered from `ui` or a future PixiJS layer.
