# engine/engines/CharacterEngine

Tracks who is on stage, in what expression/position, and who's currently speaking. Character metadata (name, color, available expressions) is registered once at boot via `registerCharacters`.
