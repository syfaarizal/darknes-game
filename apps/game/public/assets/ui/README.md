# assets/ui

UI chrome, split into `buttons/`, `icons/`, `frames/`, `dialogue/`, `choice/`, `menu/` — matches the folders `@darknes/ui` components expect when they move from CSS-only styling to asset-backed chrome (e.g. a carved dialogue-box frame image).
