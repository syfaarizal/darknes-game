# assets/backgrounds

One folder per location (`office`, `basement`, `living-room`, `library`). Convention: `<location>/default.webp`, or `<location>/<variant>.webp` for time-of-day / damage-state variants.
