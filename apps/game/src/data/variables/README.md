# data/variables

Default/initial values for numeric or string story variables (e.g.
`trustNathael: 0`, `suspicionLevel: 0`), loaded into `gameStore.variables`
on New Game. Empty for now — add a `defaults.json` here as soon as the
story needs tracked numeric state beyond simple flags.
