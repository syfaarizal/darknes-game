# data/flags

Documents every boolean flag the story can set (e.g. `toldTruth`,
`walkedAway`), so writers have a single reference instead of grepping scene
JSON for `setFlags`. Empty for now — add a `registry.json` (flag id →
description) as the branching story grows past what's easy to keep in
your head.
