# constants

App-only constants (e.g. the first-scene id used by "New Game", route path
strings) that don't belong in `@darknes/shared`. Empty for now beyond what's
inlined in `pages/MainMenu` — extract here once more than one place needs a
value.
