# pages/Game

The core gameplay screen: Camera > Background + CharacterLayer, DialogueLayer, TopBar, BottomBar, HistoryLog, Notification, ScreenFade — everything driven by `useDialogueRunner()` and the current scene node.
