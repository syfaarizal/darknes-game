# types

App-only TypeScript types that don't belong in `@darknes/shared` because no
other package needs them (e.g. route param types, page-local prop types).
Empty for now — most types live in `@darknes/shared` by design.
